return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    filesystem = {
      filtered_items = {
        visible = true, -- Exibir arquivos ocultos
        hide_gitignored = false, -- Não ocultar arquivos ignorados pelo Git
      },
    },
  },
}

